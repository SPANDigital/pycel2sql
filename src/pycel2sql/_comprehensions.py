"""Comprehension handler (all/exists/map/filter).

The comprehension handling is integrated directly into the Converter class
in _converter.py. This module exists as a placeholder for potential future
extraction if the comprehension logic grows complex enough to warrant its own module.
"""
