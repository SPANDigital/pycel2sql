"""JSON/JSONB field detection and path building.

The JSON handling is integrated directly into the Converter class in _converter.py.
This module exists as a placeholder for potential future extraction if the JSON logic
grows complex enough to warrant its own module.
"""
